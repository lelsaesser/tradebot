spring:
  task:
    scheduling:
      enabled: false
  datasource:
    url: jdbc:sqlite:data/tradebot-dev.db

tradebot:
  api:
    finnhub-key: ${FINNHUB_DEV_API_KEY:}
    coingecko-key: ${COINGECKO_DEV_API_KEY:}
    twelvedata-key: ${TWELVEDATA_DEV_API_KEY:}
  telegram:
    local-sink-file: config/dev-telegram-messages.log
