info:
  name: sectorRotation
  type: http
  seq: 9

http:
  method: POST
  url: http://localhost:9090/dev/jobs/sector-rotation
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
