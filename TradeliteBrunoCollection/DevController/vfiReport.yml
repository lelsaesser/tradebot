info:
  name: vfiReport
  type: http
  seq: 14

http:
  method: POST
  url: http://localhost:9090/dev/jobs/vfi-report
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
