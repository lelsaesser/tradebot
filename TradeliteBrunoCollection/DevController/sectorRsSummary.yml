info:
  name: sectorRsSummary
  type: http
  seq: 10

http:
  method: POST
  url: http://localhost:9090/dev/jobs/sector-rs-summary
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
