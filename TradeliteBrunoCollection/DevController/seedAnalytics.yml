info:
  name: seedAnalytics
  type: http
  seq: 11

http:
  method: POST
  url: http://localhost:9090/dev/jobs/seed-analytics
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
