info:
  name: hourlySignals
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:9090/dev/jobs/hourly-signals
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
