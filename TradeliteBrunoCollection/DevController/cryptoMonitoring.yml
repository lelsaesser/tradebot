info:
  name: cryptoMonitoring
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:9090/dev/jobs/crypto-monitoring
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
