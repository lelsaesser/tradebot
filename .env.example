# Default runtime credentials (used when no Spring profile is set)
FINNHUB_API_KEY=
COINGECKO_API_KEY=
TELEGRAM_BOT_TOKEN=
TELEGRAM_BOT_GROUP_CHAT_ID=

# Optional local development profile
# SPRING_PROFILES_ACTIVE=dev

# Development live API credentials (used only with profile: dev)
# Create/provide these separately from the default runtime credentials.
FINNHUB_DEV_API_KEY=
COINGECKO_DEV_API_KEY=
